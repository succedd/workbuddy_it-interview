/* =========================================================================
 *  js/docs/dba.js — 技术教程「数据库 / DBA」方向数据（标准实战篇）
 *  风格：原理 → 实战 → 踩坑 → 排障清单。正文 Markdown，复用 marked + highlight.js。
 *  正文代码围栏用 ${F}、行内代码用 ${C}；shell/SQL 里 ${C}VAR${C} 写成 \${VAR}。
 * ========================================================================= */
(function () {
  "use strict";
  const F = "\u0060\u0060\u0060";   // 代码块围栏 ```
  const C = "\u0060";               // 行内代码 `

  const DBA = {
    id: "dba",
    name: "数据库 / DBA",
    icon: "🗄️",
    desc: "关系型与 NoSQL 的原理、调优、高可用与数据治理。覆盖 SQL 优化、索引执行计划、事务隔离、主从复制、Redis 持久化到分库分表与容灾。",
    levels: [
      /* ============================ 初级 ============================ */
      {
        id: "basic",
        name: "初级",
        desc: "会写会查、懂基本运维：写好 SQL、理解范式、会备份恢复、管好账号权限。",
        chapters: [
          {
            id: "sql-basics",
            title: "SQL 基础与查询优化入门",
            minutes: 15,
            updated: "2026-09-15",
            applies: "MySQL 8.0 / PostgreSQL 15",
            tags: ["SQL", "查询", "优化"],
            terms: ["SQL", "查询", "MySQL", "索引"],
            body: `
## 一、SQL 是声明式语言：你说要什么，数据库决定怎么拿

和写程序「一步步告诉计算机怎么做」不同，SQL 只描述**结果集长什么样**。优化器负责选择执行计划。所以「为什么慢」往往出在执行计划，而不是 SQL 文本本身。

## 二、单表查询的核心算子

${F}sql
SELECT u.name, COUNT(o.id) AS cnt
FROM users u
JOIN orders o ON o.user_id = u.id
WHERE o.created_at >= '2026-09-01'   -- 先过滤，越早越好
GROUP BY u.name
HAVING cnt > 3                        -- 分组后再筛选用 HAVING
ORDER BY cnt DESC
LIMIT 10;
${F}

执行顺序回顾：**FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT**。理解它就知道为什么 WHERE 里不能用 SELECT 别名。

## 三、让查询快起来的第一性原理

**减少扫描行数 + 减少回表 + 减少临时排序**：

- WHERE 条件尽量命中索引（见索引篇）
- 只 SELECT 需要的列，避免 ${C}SELECT *${C}
- ORDER BY / GROUP BY 的字段尽量覆盖在索引里，避免 filesort/temporary

## 四、EXPLAIN 入门三连

${F}sql
EXPLAIN SELECT ...;
-- type：访问类型，ALL(全表) < index < range < ref < const
-- key：实际走的索引
-- rows：预估扫描行数（越小越好）
-- Extra：Using filesort / Using temporary 是告警信号
${F}

## ⚠ 踩坑与经验

1. **${C}SELECT *${C} 还 ORDER BY 大字段**：拉回大量数据再排序，内存和带宽都亏。
2. **LIMIT 深分页**：${C}LIMIT 100000,20${C} 要扫 10 万行，用「上一页末 id + LIMIT」游标分页。
3. **在 WHERE 对列套函数**：${C}WHERE YEAR(created_at)=2026${C} 用不上索引，改成范围查询。
4. **不分清热点**：把低频维度和高频维度混一起查，索引难建，拆开查再聚合。
5. **滥用子查询**：很多子查询改写成 JOIN 后性能翻倍，优化器对 JOIN 更友好。

## ✅ 排障清单

- [ ] 只取需要的列，不用 SELECT *
- [ ] WHERE 命中索引，不对列套函数
- [ ] 深分页改游标分页
- [ ] 改完 EXPLAIN 看 type/key/rows/Extra
- [ ] 子查询优先改 JOIN 验证性能
`
          },
          {
            id: "schema-normalization",
            title: "数据库对象设计与范式",
            minutes: 14,
            updated: "2026-09-15",
            applies: "关系型数据库通用",
            tags: ["范式", "设计", "建模"],
            terms: ["范式", "数据库", "设计", "建模"],
            body: `
## 一、范式：减少冗余与异常的纪律

- **1NF**：字段原子化（别把「地址」塞一个字段还指望按省市查）
- **2NF**：非主键字段完全依赖主键（消除部分依赖）
- **3NF**：非主键字段不依赖其他非主键字段（消除传递依赖）

范式的好处：更新只改一处，不会「同一份数据多个副本对不上」。

## 二、但别死磕范式：反范式也是武器

过度范式化 → 查询要 JOIN 一大串，读性能差。实际工程常**适度反范式**：把高频查询需要的字段冗余到主表，用空间换读取速度。

${F}sql
-- 订单表冗余 user_name，避免每次都 JOIN users
SELECT order_no, user_name, amount FROM orders WHERE ...;
${F}

代价是写入时要维护冗余一致性（或用触发器/异步补偿）。**读写比例决定反范式程度**：读多写少，反范式更划算。

## 三、选型：字段类型决定一半性能

| 场景 | 选错 | 选对 |
|---|---|---|
| 状态/枚举 | VARCHAR | TINYINT |
| 金额 | FLOAT | DECIMAL |
| 长文本 | VARCHAR(大) | TEXT |
| 主键 | UUID | BIGINT 自增 / 雪花 |
| 时间 | VARCHAR | DATETIME/TIMESTAMP |

⚠ **UUID 当主键的坑**：离散插入导致 InnoDB 页分裂、索引碎片严重，顺序写入性能远不如自增。要用 UUID 建议用有序 UUID 或雪花 ID。

## ⚠ 踩坑与经验

1. **滥用 VARCHAR(255) 当万能类型**：索引长度、内存占用都吃亏，按真实长度定。
2. **NULL 满天飞**：NULL 参与运算/比较都特殊，索引和统计都别扭，能用默认值就用默认值。
3. **过度范式 JOIN 10 张表**：读性能崩，适当反范式冗余。
4. **主键用 UUID**：索引碎片 + 写入随机 IO，能避则避。
5. **把业务含义塞进一个字段**：后续查询、统计全废，尽早拆原子字段。

## ✅ 排障清单

- [ ] 设计满足 3NF，再按需反范式优化读
- [ ] 金额 DECIMAL、状态 TINYINT、时间用日期类型
- [ ] 主键优先自增 BIGINT / 有序 ID，慎用 UUID
- [ ] 避免 NULL 滥用，能用默认值替代
- [ ] 读写比高时适度冗余高频字段
`
          },
          {
            id: "backup-restore",
            title: "备份与恢复基本操作",
            minutes: 14,
            updated: "2026-09-15",
            applies: "MySQL 8.0 / mysqldump / XtraBackup",
            tags: ["备份", "恢复", "容灾"],
            terms: ["备份", "恢复", "容灾", "MySQL"],
            body: `
## 一、备份的核心命题：能恢复才算备份

**没演练过的备份等于没有备份。** 我见过太多「每天自动备份，真出事发现备份是坏的」的惨案。备份必须定期**恢复演练**。

## 二、两种备份形态

| 类型 | 说明 | 恢复速度 | 适用 |
|---|---|---|---|
| 逻辑备份 | ${C}mysqldump${C} 导出 SQL | 慢（要 replay） | 小库、跨版本迁移 |
| 物理备份 | XtraBackup 拷数据文件 | 快（直接拷回） | 大库、生产主备 |

${F}bash
# 逻辑备份单库
mysqldump -u root -p --single-transaction mydb > mydb.sql
# 物理备份（热备，不锁表）
xtrabackup --backup --target-dir=/backup/$(date +%F)
${F}

## 三、binlog：把「时间点恢复」变成可能

全量备份 + binlog，可以恢复到**任意一个时间点**（PITR）。原理：从全量备份恢复，再回放全量之后到故障前的 binlog。

${F}bash
# 找到故障前最后一个位置，回放 binlog 到那
mysqlbinlog --stop-datetime="2026-09-15 14:00:00" mysql-bin.000123 | mysql -u root -p
${F}

## 四、恢复演练 checklist

- 备份文件完整性校验（大小/checksum）
- 定期在隔离环境 restore 一次，验证能起来
- 记录 RTO（恢复要多久）和 RPO（最多丢多少数据）——这两个指标比「有没有备份」重要

## ⚠ 踩坑与经验

1. **只备份不演练**：备份脚本静默失败 months，出了事才发现文件是空的。
2. **没开 binlog**：只能恢复到「上次全备」，中间几天数据全丢，RPO 崩了。
3. **备份锁表把线上拖死**：大库用 ${C}--single-transaction${C} 或物理热备，别用锁表dump。
4. **恢复目标选错时间点**：binlog 回放过头，把「删除操作」也重放了。先确认 stop 点。
5. **备份和主库放同一块盘**：盘坏了备份一起没，异地/对象存储才保险。

## ✅ 排障清单

- [ ] 全量 + binlog 才能做时间点恢复（PITR）
- [ ] 定期真实 restore 演练，验证可用性
- [ ] 明确 RTO / RPO 指标并监控
- [ ] 大库用热备，避免锁表
- [ ] 备份异地/对象存储，不与主库同盘
`
          },
          {
            id: "user-privilege",
            title: "用户与权限管理",
            minutes: 12,
            updated: "2026-09-15",
            applies: "MySQL 8.0",
            tags: ["权限", "安全", "账号"],
            terms: ["权限", "账号", "MySQL", "安全"],
            body: `
## 一、最小权限原则：给刚好够用的权

DBA 的第一安全意识：**一个应用账号，只给它需要的库表、需要的操作**。别用 root 跑应用。

${F}sql
-- 创建应用账号，只允许从应用网段连，只给某库增删改查
CREATE USER 'app'@'10.0.1.%' IDENTIFIED BY '强密码';
GRANT SELECT, INSERT, UPDATE, DELETE ON shop.* TO 'app'@'10.0.1.%';
FLUSH PRIVILEGES;
${F}

## 二、MySQL 8 的认证插件变了

MySQL 8 默认用 ${C}caching_sha2_password${C}，老驱动/老客户端（如某些旧 JDBC）可能连不上。要么升级驱动，要么建账号时指定 ${C}mysql_native_password${C}（权衡安全性）。

## 三、角色（Role）：权限的「包」

MySQL 8 支持角色，把一组权限打包：

${F}sql
CREATE ROLE 'reader', 'writer';
GRANT SELECT ON shop.* TO 'reader';
GRANT SELECT, INSERT, UPDATE, DELETE ON shop.* TO 'writer';
GRANT 'reader' TO 'analyst'@'%';
${F}

## ⚠ 踩坑与经验

1. **应用用 root 连库**：一旦应用被攻破，库被全删。最小权限是底线。
2. **账号没限制来源 host**：${C}'app'@'%'${C} 允许从任意 IP 连，等于把库暴露到公网。限定网段。
3. **密码写进代码仓库**：用密钥管理/配置中心，别提交明文。
4. **GRANT 后忘 FLUSH PRIVILEGES**：部分场景改系统表才需要，但用 GRANT 语句通常自动生效，老习惯导致困惑。
5. **离职/下线账号不回收**：过期账号长期留着，扩大攻击面。

## ✅ 排障清单

- [ ] 应用账号最小权限，限定来源 host 网段
- [ ] 不用 root 跑应用，按角色管理权限
- [ ] 密码走密钥管理，不进仓库
- [ ] MySQL 8 注意认证插件与驱动兼容
- [ ] 定期回收无用/离职账号
`
          }
        ]
      },
      /* ============================ 中级 ============================ */
      {
        id: "mid",
        name: "中级",
        desc: "能独立优化与排障：吃透执行计划、事务隔离与锁、慢查询治理、主从复制、Redis 内部机制。",
        chapters: [
          {
            id: "index-execplan",
            title: "索引原理与执行计划",
            minutes: 18,
            updated: "2026-09-15",
            applies: "MySQL 8.0（InnoDB）",
            tags: ["索引", "执行计划", "B+树"],
            terms: ["索引", "执行计划", "B+树", "MySQL"],
            body: `
## 一、为什么是 B+ 树

InnoDB 索引用 **B+ 树**：矮胖（3~4 层就能存千万级数据）、叶子节点用链表串起来（范围查询极快）、非叶子节点只存 key（单页能放更多指针，树更矮）。相比哈希索引，B+ 树支持范围；相比二叉/B 树，更适配磁盘块。

## 二、聚簇索引 vs 二级索引

- **聚簇索引（主键）**：叶子节点存**整行数据**。InnoDB 表就是按主键排的。
- **二级索引**：叶子节点存**主键值**，查到主键后再「回表」拿整行。

⚠ 这就是为什么**回表**有代价：二级索引查到主键，再走聚簇索引取数据。覆盖索引（要的字段都在索引里）能省掉回表，显著提速。

## 三、索引怎么选、怎么建

- **高区分度列**放联合索引前面（性别这种低区分度建了也几乎不走）
- **最左前缀**：(a,b,c) 能服务 a、a+b、a+b+c 的查询
- **覆盖索引**：把查询列都并进索引，避免回表
- **前缀/哈希**：长字符串用 ${C}INDEX(col(20))${C} 或加哈希列

${F}sql
-- 这个查询能走 (user_id, status) 且覆盖 created_at 吗？看 EXPLAIN 的 Extra
EXPLAIN SELECT status, created_at FROM orders WHERE user_id = 100;
${F}

## 四、EXPLAIN 进阶：Extra 列的信号

| Extra | 含义 | 行动 |
|---|---|---|
| Using index | 覆盖索引，好 | 保持 |
| Using where | 回表后还过滤 | 看能否并入索引 |
| Using filesort | 额外排序，慢 | 排序字段进索引 |
| Using temporary | 用临时表，慢 | 改写/group by 优化 |
| Using index condition | 索引下推 ICP，好 | 8.0 默认 |

## ⚠ 踩坑与经验

1. **索引建太多**：写入变慢、缓冲池被索引占满，优化器还可能选错。
2. **联合索引列序错**：把低区分度/不常过滤的列放前面，最左前缀浪费。
3. **隐式类型转换**：varchar 字段 where 传数字，索引失效。
4. **对索引列用函数/运算**：索引失效，改成范围或函数索引（8.0 支持）。
5. **迷信 EXPLAIN 的 rows**：那是估算，结合实际耗时与慢日志判断。

## ✅ 排障清单

- [ ] 主键聚簇、二级索引回表，争取覆盖索引
- [ ] 联合索引最左前缀，列序按区分度+过滤频率
- [ ] EXPLAIN 看 type/key/rows/Extra，消除 filesort/temporary
- [ ] 避免隐式转换、列上函数导致索引失效
- [ ] 索引数量权衡写入与查询，不盲目堆
`
          },
          {
            id: "tx-isolation-lock",
            title: "锁与事务隔离级别",
            minutes: 17,
            updated: "2026-09-15",
            applies: "MySQL 8.0（InnoDB）",
            tags: ["事务", "锁", "隔离级别"],
            terms: ["事务", "锁", "隔离级别", "死锁"],
            body: `
## 一、事务的 ACID 与隔离级别的权衡

| 隔离级别 | 脏读 | 不可重复读 | 幻读 | 性能 |
|---|---|---|---|---|
| 读未提交 | 可能 | 可能 | 可能 | 最高 |
| 读已提交 RC | 无 | 可能 | 可能 | 高 |
| 可重复读 RR（MySQL 默认） | 无 | 无 | 基本无 | 中 |
| 串行化 | 无 | 无 | 无 | 最低 |

InnoDB 在 RR 下用 **MVCC + 间隙锁**基本解决了幻读，所以多数业务用默认的 RR 即可。

## 二、MVCC：不加锁也能读到一致快照

InnoDB 给每行加隐藏的事务 ID 和回滚指针，读操作按「自己的事务版本」读快照，**读不加锁、读写不互相阻塞**。这就是「快照读」，是高并发下读性能的根本。

${C}当前读${C}（${C}SELECT ... FOR UPDATE${C}、UPDATE）才加锁，用于要改的数据。

## 三、InnoDB 的锁类型

- **行锁**：锁具体索引记录（没走索引的「行锁」会升级成锁全表！）
- **间隙锁（Gap）**：锁索引记录之间的间隙，防插入（RR 下）
- **Next-Key Lock** = 行锁 + 间隙锁，防幻读
- **意向锁**：表级，标识「即将在行上加锁」，避免表锁和行锁冲突

⚠ **没走索引的 UPDATE/DELETE 会锁全表**：这是线上「一条语句把整张表锁死」的头号原因。

## 四、死锁：两个事务互相等待

${C}A 锁了行1等行2，B 锁了行2等行1${C} → 死锁。InnoDB 会检测并回滚代价小的一方。应用层要**统一加锁顺序**减少死锁，且**捕获死锁异常重试**。

## ⚠ 踩坑与经验

1. **UPDATE 没走索引 → 锁全表**：先 EXPLAIN 确认走索引，否则并发下直接卡死。
2. **长事务占着锁不放**：事务里做远程调用/大计算，锁持有过久，别人全阻塞。
3. **死锁不处理**：不捕获重试，业务直接报错失败。统一加锁顺序 + 重试。
4. **RC 下误以为可重复读**：业务依赖「读两次一致」却用 RC，中间被别人改了。
5. **把大事务当小事**：事务越大，锁范围/持有时间越长，越大概率撞锁。

## ✅ 排障清单

- [ ] 确认 UPDATE/DELETE 走索引，避免锁升级全表
- [ ] 事务尽量短，不在事务里做 IO/远程调用
- [ ] 统一加锁顺序，捕获死锁异常重试
- [ ] 按业务需要选隔离级别（多数 RR 够）
- [ ] 理解 MVCC 快照读 vs 当前读
`
          },
          {
            id: "slow-query",
            title: "慢查询治理",
            minutes: 16,
            updated: "2026-09-15",
            applies: "MySQL 8.0",
            tags: ["慢查询", "治理", "监控"],
            terms: ["慢查询", "SQL", "优化", "MySQL"],
            body: `
## 一、先让慢查询「现形」

不开慢日志，慢 SQL 就藏在黑盒里。MySQL 自带：

${F}sql
SET GLOBAL slow_query_log = ON;
SET GLOBAL long_query_time = 1;        -- 超过 1 秒记慢日志
SET GLOBAL slow_query_log_file = '/var/log/mysql/slow.log';
${F}

配合 **pt-query-digest** 汇总分析，找出「最耗时 / 出现最频繁」的 TOP SQL——别一上来优化所有，先打头部 20%。

## 二、慢查询的常见根因（按频率）

1. **全表扫描（type=ALL）**：没索引或索引失效
2. **深分页**：${C}LIMIT 100000,20${C}
3. **N+1 查询**：循环里逐条查库
4. **隐式转换 / 函数包裹**：索引失效
5. **大事务 + 锁等待**：慢的不是 SQL 本身，是等锁
6. **返回过多列/行**：${C}SELECT *${C} + 没 LIMIT

## 三、优化套路（可复用）

${F}text
1. slow log 抓 TOP SQL
2. EXPLAIN 看执行计划，定位扫描行数/是否回表/排序
3. 加/调整索引（联合、覆盖）
4. 改写 SQL（JOIN 替代子查询、游标分页、只取所需列）
5. 改不了 SQL 就改结构（冗余字段、汇总表）
6. 复测：同量数据下耗时是否下降
${F}

## 四、长治久安：把治理做成机制

- 上线前 **SQL 审核**（如 Yearning / Archery），拦截全表扫描、没索引的语句
- 慢日志 + 监控告警（慢查询数突增即报警）
- 定期 review 执行计划变化的 SQL

## ⚠ 踩坑与经验

1. **long_query_time 设太大**：设 5 秒，3 秒的慢 SQL 全漏了，先用 1 秒抓全。
2. **只优化一条漏了同类**：同一个写法在 10 张表，要批量治理。
3. **加索引反而更慢**：索引太多，写入和更新变重，优化器选错。权衡。
4. **N+1 在 ORM 里隐藏**：框架自动逐条查，肉眼看代码发现不了，看慢日志才暴露。
5. **优化完不复测**：以为加索引就好，实际还是 filesort，没验证等于没做。

## ✅ 排障清单

- [ ] 开启 slow log + long_query_time，pt-query-digest 抓 TOP
- [ ] EXPLAIN 定位扫描/回表/排序
- [ ] 根因多为缺索引、深分页、N+1、隐式转换
- [ ] 上线前 SQL 审核拦截风险语句
- [ ] 优化后复测执行计划与耗时
`
          },
          {
            id: "replication",
            title: "主从复制与读写分离",
            minutes: 16,
            updated: "2026-09-15",
            applies: "MySQL 8.0（binlog 复制）",
            tags: ["主从", "读写分离", "复制"],
            terms: ["主从", "读写分离", "复制", "MySQL"],
            body: `
## 一、主从复制解决什么问题

- **读扩展**：一主多从，读流量分摊到从库
- **高可用**：主挂了切从库继续服务
- **备份/分析**：从库做备份和离线分析，不影响主库

## 二、复制原理（基于 binlog）

${C}主库写 binlog → 从库 IO 线程拉 binlog 到本地 relay log → SQL 线程回放${C}

- 默认**异步复制**：主库提交后不等从库，性能高但有延迟
- **半同步**：至少一个从库接收并落盘才返回，降低丢数据风险（牺牲一点性能）
- **组复制 MGR**：多节点一致，自动选主

## 三、读写分离的典型架构

应用 → 中间件（MyCat / ShardingSphere / ProxySQL）→ 按 SQL 类型路由：写走主、读走从。

⚠ **主从延迟是读写分离的阿喀琉斯之踵**：从库落后主库几秒，刚写完立刻读从库可能读不到（「读己之写」失败）。对策：① 写后强读走主库；② 关键路径忽略延迟；③ 用 Session 级一致性读。

## 四、复制延迟怎么查、怎么降

${F}sql
SHOW SLAVE STATUS\\G
-- Seconds_Behind_Master：延迟秒数
-- 大事务、从库硬件差、单线程回放都会导致延迟
${F}

降延迟：避免主库大事务（拆小）、从库用多线程回放（MTS）、从库硬件对齐主库。

## ⚠ 踩坑与经验

1. **读写分离后「刚写入读不到」**：主从延迟，关键读回主库或加一致性策略。
2. **大事务导致从库延迟暴涨**：一个几 GB 的事务，从库回放要很久，延迟雪崩。
3. **从库当主库用还写数据**：从库被写，主从数据冲突，复制断掉。从库只读。
4. **Slave 断了没人发现**：没监控复制状态，数据长时间不一致。监控 Seconds_Behind_Master + 线程状态。
5. **半同步降级不自知**：网络抖动态下退回异步，可能丢数据，要有监控和降级预案。

## ✅ 排障清单

- [ ] 读写分离处理主从延迟（写后强读走主）
- [ ] 从库只读，避免被写导致复制冲突
- [ ] 监控 Seconds_Behind_Master 与 Slave 线程状态
- [ ] 大事务拆小，降低从库延迟
- [ ] 按可用性选异步/半同步/MGR，并配降级预案
`
          },
          {
            id: "redis-internals",
            title: "Redis 数据结构与持久化",
            minutes: 17,
            updated: "2026-09-15",
            applies: "Redis 6 / 7",
            tags: ["Redis", "数据结构", "持久化"],
            terms: ["Redis", "持久化", "RDB", "AOF"],
            body: `
## 一、5 种基础结构不只是「5 种」

Redis 对外 5 种类型（String/Hash/List/Set/ZSet），但底层有更丰富的编码（ziplist/listpack、skiplist、intset、hashtable），会根据数据量自动切换。理解编码有助于判断内存与性能。

| 类型 | 典型用途 | 坑 |
|---|---|---|
| String | 缓存、计数器、分布式锁 | 大 value（>10KB）网络/内存成本高 |
| Hash | 对象属性（用户信息） | field 过多变 hashtable，注意阈值 |
| List | 队列、最新列表 | 用 LPUSH+LRANGE 做栈/队列 |
| Set | 去重、交集（共同好友） | — |
| ZSet（有序集合） | 排行榜、延迟队列 | 跳表，范围查询快 |

## 二、持久化：RDB vs AOF

- **RDB**：某时刻全量快照。恢复快、文件小，但**可能丢最后一次快照后的数据**。适合做备份。
- **AOF**：记录每条写命令，可做到秒级丢失。文件大、恢复慢，但更可靠。Redis 7 的 AOF 重写更紧凑。
- **混合持久化**（推荐）：AOF 文件头是 RDB 快照 + 增量 AOF，兼顾恢复速度与安全。

${F}bash
# redis.conf 关键配置
save 900 1            # 900 秒内至少 1 次改动则快照
appendonly yes
appendfsync everysec  # 每秒刷盘，崩溃最多丢 1 秒
${F}

## 三、过期与淘汰：内存不是无限的

- **过期策略**：惰性删除（访问时才删）+ 定期抽样删除
- **内存满了（maxmemory）**：按淘汰策略删：${C}allkeys-lru${C}（全量 LRU）、${C}volatile-lru${C}（只删有过期的）、${C}noeviction${C}（不删，写报错）

⚠ **用了 Redis 当缓存却设 noeviction**：内存满后写操作直接报错，缓存反而成了故障源。缓存场景用 allkeys-lru/lfu。

## ⚠ 踩坑与经验

1. **大 key**：一个 value 几十 MB 或 Set 上百万成员，删除/序列化卡住单线程。拆 key、用 scan 渐进删除。
2. **热 key**：某个 key QPS 极高，单线程瓶颈。本地缓存 + 多副本分散。
3. **缓存当数据库、没开持久化**：Redis 重启全丢，下游被打挂。
4. **淘汰策略配错**：noeviction + 内存满 = 写报错，缓存雪崩。
5. **KEYS ***：线上用 ${C}KEYS *${C} 遍历全库，阻塞服务，用 ${C}SCAN${C} 渐进替代。

## ✅ 排障清单

- [ ] 缓存场景用 allkeys-lru/lfu，别用 noeviction
- [ ] 开混合持久化，平衡恢复速度与数据安全
- [ ] 避免大 key / 热 key，用 SCAN 替代 KEYS
- [ ] 明确 Redis 是加速层，库才是真相源
- [ ] 监控内存与驱逐，防缓存雪崩
`
          }
        ]
      },
      /* ============================ 高级 ============================ */
      {
        id: "adv",
        name: "高级",
        desc: "能设计数据架构：分库分表、高可用容灾、容量规划、平滑迁移与一致性保障。",
        chapters: [
          {
            id: "sharding-middleware",
            title: "分库分表与中间件",
            minutes: 19,
            updated: "2026-09-15",
            applies: "ShardingSphere / MyCat",
            tags: ["分库分表", "中间件", "ShardingSphere"],
            terms: ["分库分表", "ShardingSphere", "中间件", "MySQL"],
            body: `
## 一、分库分表：把大象装进冰箱的哲学

单表上千万、单机写入/连接数到顶，就得拆。两层：
- **分库**：分散到不同实例，突破单机连接/CPU/磁盘上限
- **分表**：单表行数降下来，索引维护、备份都变快

## 二、分片策略

| 策略 | 做法 | 注意 |
|---|---|---|
| 哈希分片 | ${C}hash(key) % N${C} | 数据均匀，但范围查询需扫全片 |
| 范围分片 | 按时间/ID 区间 | 易倾斜（新数据全落最新片） |
| 一致性哈希 | 哈希环 | 扩缩容影响小，适合缓存 |

**分片键选择**是灵魂（Java 篇讲过）：选查询常带、高基数的字段（user_id/order_id），否则大量「广播查询」扫所有片。

## 三、中间件的职责

ShardingSphere 这类中间件在应用和 DB 之间做**SQL 解析 → 路由 → 结果归并**：

${F}text
SELECT * FROM orders WHERE user_id=100;   -- 中间件算出落在片 3，只查片 3
SELECT * FROM orders WHERE amount>100;    -- 无分片键，广播到所有片再归并
${F}

⚠ **分布式主键**：自增 ID 在多片会冲突，要用**雪花算法（Snowflake）**等全局唯一 ID。

## 四、带来的新难题

- **跨片 JOIN**：尽量避免，用「冗余维度表」或应用层多次查再聚合
- **分布式事务**：强一致用 Seata，多数用最终一致（见 Java 分布式事务篇）
- **扩缩容**：分片数一旦定死，再扩要数据迁移，新项目分片数留足余量

## ⚠ 踩坑与经验

1. **分片键选低基数字段**：数据严重倾斜，某些片巨胖、某些空。
2. **滥用广播查询**：不带分片键的 SQL 扫全片，分片越多越慢。
3. **自增主键跨片冲突**：必须用全局 ID（雪花），否则插入冲突。
4. **过早分片**：加索引、读写分离能解决时先别分，复杂度爆炸。
5. **分片数定太小**：后期数据再涨没法扩，迁移成本极高，一次留足（如 64/128）。

## ✅ 排障清单

- [ ] 分片键选高基数、查询常带字段
- [ ] 避免跨片 JOIN / 分布式事务，必要时冗余+最终一致
- [ ] 全局主键用雪花等，禁自增跨片
- [ ] 先索引+读写分离，确到瓶颈再分片
- [ ] 分片数留足余量，降低后期迁移成本
`
          },
          {
            id: "ha-dr",
            title: "高可用与容灾方案",
            minutes: 18,
            updated: "2026-09-15",
            applies: "MySQL / 云 RDS",
            tags: ["高可用", "容灾", "MHA", "MGR"],
            terms: ["高可用", "容灾", "MGR", "主从"],
            body: `
## 一、先定义目标：RTO 与 RPO

- **RTO（恢复时间目标）**：故障后多久能恢复服务
- **RPO（恢复点目标）**：最多丢多少数据

这两个指标决定你用多豪华的方案。金融级要求 RPO≈0、RTO 秒级；普通业务可接受分钟级。

## 二、高可用演进路径

${C}主从 + 手动切换 → MHA/Orchestrator 自动切换 → MGR 多副本自动选主 → 云 RDS 托管${C}

**MGR（MySQL Group Replication）**：多个节点组成复制组，基于 Paxos 选主，数据强一致（单主模式最常用），节点故障自动剔除、恢复自动加入。8.0 强烈推荐。

## 三、容灾：同城 + 异地

- **同城多活/主备**：同城市多机房，网络延迟低，做同步或半同步
- **异地灾备**：不同城市，异步复制，应对城市级故障（地域灾难）
- **两地三中心**：生产中心 + 同城灾备 + 异地灾备，金融标配

⚠ **异地异步复制的 RPO 不零**：主城灾难时异地可能丢几秒数据，要业务可接受或配合补偿。

## 四、故障切换的隐藏成本

切换不是「主库挂了切从库」一句话。要考虑：
- 旧主恢复后脑裂（fencing）：确保旧主真的下线再提升新主
- 应用连接串要自动指向新主（VIP / 域名 / 配置中心）
- 切换后 DNS 缓存、连接池里的旧连接要清理

## ⚠ 踩坑与经验

1. **脑裂**：旧主没真正隔离又被写，数据分叉。用 fencing / 仲裁节点防脑裂。
2. **切换后连接池还连旧主**：连接池缓存旧连接，报错一片。切换要刷新连接。
3. **只做高可用不做容灾**：同城机房一起故障（断电/断网）就全完，异地才防城市级灾难。
4. **RPO 想当然为零**：异步复制异地有延迟，真灾难会丢数据，业务要知情。
5. **演练从不做**：切换脚本从来没真跑过，出事手忙脚乱，预案等于没有。

## ✅ 排障清单

- [ ] 明确 RTO/RPO，据此选方案（MGR / 云 RDS）
- [ ] 用 fencing 防脑裂，切换后刷新连接池
- [ ] 同城 + 异地容灾，异地异步需业务容忍 RPO
- [ ] 切换预案定期真实演练
- [ ] 连接串走 VIP/域名/配置中心，切换自动生效
`
          },
          {
            id: "capacity-bench",
            title: "容量规划与压测",
            minutes: 16,
            updated: "2026-09-15",
            applies: "数据库容量规划",
            tags: ["容量", "压测", "规划"],
            terms: ["容量", "压测", "规划", "性能"],
            body: `
## 一、DB 容量规划的特殊性

DB 比无状态服务难扩：有状态、要保数据。所以**提前规划比事后扩容重要得多**。

核心约束：
- **连接数**：实例有最大连接上限（如 2000），应用实例数 × 每实例连接池不能超
- **CPU/IO**：写放大、慢 SQL 都会打满
- **磁盘**：数据量增长 + binlog/redo 留存

## 二、压测 DB 的正确姿势

${F}bash
# 用 sysbench 模拟 OLTP 读写
sysbench oltp_read_write --db-driver=mysql --threads=64 \\
  --tables=10 --table-size=1000000 prepare
sysbench oltp_read_write --threads=64 --time=300 run
${F}

重点观察：**QPS/TPS 拐点、P99 延迟、连接数、CPU、磁盘 IO、慢日志是否飙升**。逐步加压找容量上限。

## 三、常见容量杀手（DB 视角）

1. **连接池过大**：50 应用 × 50 连接 = 2500，超实例上限，新连接被拒
2. **慢 SQL**：一个全表扫占满连接，传染全站
3. **热点行**：单条记录被高频更新（如热门商品库存），行锁竞争
4. **大事务**：锁范围大、binlog 暴涨
5. **索引缺失**：扫描行数爆炸

## 四、规划口诀

${C}单实例容量（压测得） - 预留余量(30%) = 可用容量；按业务增长预测到顶时间，提前扩容/分片${C}

## ⚠ 踩坑与经验

1. **按应用实例数线性算连接**：没算连接池上限，上线即「Too many connections」。
2. **压测用空表**：没造真实数据量，索引行为和生产天差地别，结论全错。
3. **只看平均 QPS**：P99 延迟早爆了，用户已在吐槽。
4. **容量算完不预留**：峰值一来就到顶，没缓冲。留 30% 余量。
5. **不分冷热数据**：全量存主库，历史数据拖慢查询，该归档归档。

## ✅ 排障清单

- [ ] 连接数 = 应用实例 × 池大小 ≤ 实例上限，留余量
- [ ] 压测造真实数据量，看 P99/连接/IO 拐点
- [ ] 热点行用分段/队列削峰，避免行锁竞争
- [ ] 定期归档冷数据，慢 SQL 是头号容量杀手
- [ ] 按增长预测扩容/分片时间点，提前动手
`
          },
          {
            id: "migration-doublewrite",
            title: "数据迁移与双写",
            minutes: 17,
            updated: "2026-09-15",
            applies: "数据库迁移通用",
            tags: ["迁移", "双写", "一致性"],
            terms: ["迁移", "双写", "数据", "一致性"],
            body: `
## 一、迁移最怕什么：丢数据、停服、不一致

无论「换数据库」「分库分表」「上云」，迁移的核心诉求都是：**不停服、不丢数据、可回滚**。

## 二、双写迁移法（经典稳妥套路）

以「老库迁新库」为例：

${F}text
阶段1 双写：应用同时写老库和新库（新库失败不影响老库）
阶段2 存量同步：把老库历史数据全量迁到新库（工具/脚本）
阶段3 校验：对比两边数据一致性（行数/抽样/checksum）
阶段4 切读：读流量逐步从老库切到新库（先小比例灰度）
阶段5 单写新库：确认稳定后停写老库，下线老库
${F}

⚠ **每一步都可回滚**：切读灰度发现问题，立刻把读切回老库。绝不一次性全切。

## 三、存量同步的工具与坑

- 全量 + 增量（CDC，如 Canal/Debezium 监听 binlog）实现「不停服迁移」
- 全量迁移时注意**对线上影响**：限速、错峰，别把源库压垮
- 大表迁移分批，避免长事务锁表

## 四、校验不能省

${F}bash
# 简单做法：分批比对行数 + 关键字段 checksum
# 更稳妥：双写期间抽样比对，切读前全量校验
${F}

## ⚠ 踩坑与经验

1. **一次性全切**：没灰度，出问题全站故障，回滚都来不及。
2. **双写只写不校验**：两边早就对不上了还不知道，切读后发现数据缺失。
3. **存量迁移把源库压垮**：全量扫大表 + 限速缺失，生产抖动。
4. **增量断点丢失**：CDC 断了没监控，增量漏数据，靠校验才发现。
5. **切读后立刻下线老库**：新库出问题没退路，保留老库一段时间兜底。

## ✅ 排障清单

- [ ] 双写 → 存量同步 → 校验 → 灰度切读 → 单写，每步可回滚
- [ ] 存量迁移限速错峰，不压垮源库
- [ ] 增量用 CDC，监控断点防漏数据
- [ ] 切读前全量校验一致性
- [ ] 新库稳定后再下线老库，保留兜底期
`
          },
          {
            id: "consistency-check",
            title: "数据一致性校验",
            minutes: 15,
            updated: "2026-09-15",
            applies: "数据治理通用",
            tags: ["一致性", "校验", "对账"],
            terms: ["一致性", "校验", "对账", "数据"],
            body: `
## 一、为什么需要一致性校验

分布式系统里，**数据最终一致是常态，但「最终」可能出岔子**：消息丢失、双写失败、迁移遗漏、缓存与库不一致。没有校验，问题会悄悄累积成大故障。

## 二、常见不一致场景

- **缓存 vs 数据库**：更新库后删缓存失败 → 缓存是旧值
- **主从**：复制中断 → 从库数据落后/错乱
- **双写两库**：一个写失败 → 两边对不上
- **跨服务**：A 成功了 B 失败了，没补偿

## 三、校验手段分层

| 层级 | 手段 | 特点 |
|---|---|---|
| 实时 | 唯一索引/约束 | 防重复插入，强一致 |
| 近实时 | 对账任务（定时比） | 分钟/小时级发现差异 |
| 离线 | 全量 checksum | 兜底，发现长期漂移 |

${F}text
# 典型对账：每日比对订单数与支付流水，差异告警
SELECT COUNT(*) FROM orders WHERE status='paid' AND id NOT IN (SELECT order_id FROM pays);
${F}

## 四、发现不一致怎么办

1. **先止血**：确认影响范围，必要时降级
2. **定位根因**：是消息丢了、双写半失败、还是校验逻辑错
3. **修复 + 补偿**：重跑丢失的消息/补偿任务，补平数据
4. **防再发**：补监控、加幂等、加告警

## ⚠ 踩坑与经验

1. **只建不校验**：以为双写/复制就一致，从不比对，半年后差异几千条。
2. **校验逻辑本身有 bug**：对账脚本条件写错，假阴性（说一致其实不一致）。
3. **发现差异不修只告警**：告警淹没，没人处理，差异越积越多。
4. **补偿不幂等**：重跑补偿又制造新不一致，补偿也要幂等。
5. **比对全量太重**：大表全量比对拖垮库，用分批/增量/抽样。

## ✅ 排障清单

- [ ] 关键链路用唯一约束/幂等防不一致
- [ ] 定时对账 + 离线 checksum 兜底，分级发现
- [ ] 校验逻辑单独验证，避免假阴性
- [ ] 发现差异先止血、查根因、幂等补偿
- [ ] 大表比对分批/抽样，不拖垮生产
`
          }
        ]
      }
    ]
  };

  window.DBA = DBA;
})();
