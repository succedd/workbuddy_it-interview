/* =========================================================================
 *  js/docs/network.js — 技术教程「网络与操作系统」方向数据（标准实战篇）
 *  风格：原理 → 实战 → 踩坑 → 排障清单。正文 Markdown，复用 marked + highlight.js。
 *  正文代码围栏用 ${F}、行内代码用 ${C}；shell 里 ${C}VAR${C} 写成 \${VAR}。
 * ========================================================================= */
(function () {
  "use strict";
  const F = "\u0060\u0060\u0060";   // 代码块围栏 ```
  const C = "\u0060";               // 行内代码 `

  const NETWORK = {
    id: "network",
    name: "网络与操作系统",
    icon: "🌐",
    desc: "所有技术方向的共同地基：搞懂数据怎么在网络上跑、进程怎么被调度、内存怎么管、IO 怎么await。这一层通透了，上层问题一眼看穿。",
    levels: [
      /* ============================ 初级 ============================ */
      {
        id: "basic",
        name: "初级",
        desc: "建立完整的基础认知：分层模型、HTTP 交互、进程线程与内存的初步理解。",
        chapters: [
          {
            id: "tcp-ip-model",
            title: "计算机网络体系与 TCP/IP",
            minutes: 14,
            updated: "2026-09-15",
            applies: "TCP/IP 协议族",
            tags: ["网络", "TCP/IP", "模型"],
            terms: ["TCP", "IP", "网络", "协议"],
            body: `
## 一、为什么要有分层

网络通信极其复杂，分层是把问题拆开：**每一层只管自己的事，向下调用、向上服务**。你寄快递不用懂货车怎么开，快递公司也不用懂你寄的是书还是手机。

## 二、两个模型：OSI 七层 vs TCP/IP 四层

面试常考，但实际工程用 **TCP/IP 四层**更实在：

| TCP/IP 层 | 对应 OSI | 干什么 | 典型协议/设备 |
|---|---|---|---|
| 应用层 | 5-7 层 | 业务数据 | HTTP/DNS/SSH |
| 传输层 | 传输层 | 端到端、可靠/不可靠 | TCP / UDP |
| 网络层 | 网络层 | 寻址、路由 | IP / ICMP |
| 网络接口层 | 1-2 层 | 相邻节点传比特 | 以太网 / Wi-Fi |

⚠ 别纠结「osi 还是 tcp/ip」，理解**每层职责边界**才是真本事。封装的本质：发数据时上层数据加上本层头部变成下层数据；收数据时逐层剥头。

## 三、一次 HTTP 请求走过的路（建立直觉）

${C}浏览器 → 应用层(HTTP) → 传输层(TCP 加端口) → 网络层(IP 加地址) → 链路层( MAC ) → 网线 → 反向逐层解包到服务器${C}

每一层都可能在出问题：DNS 解析不到（应用层）、端口不通（传输/网络）、路由不可达（网络层）、网线断了（链路层）。

## 四、IP 与端口：地址的两级

- **IP**：找到哪台机器（网络层）。IPv4 32 位，IPv6 128 位。
- **端口**：找到机器上的哪个程序（传输层）。0-1023 是系统端口（80/443/22），应用一般用 1024+。

${F}bash
# 本机监听了哪些端口
ss -lntp
# 看路由表（数据包下一跳去哪）
ip route
${F}

## ⚠ 踩坑与经验

1. **把 localhost 和 127.0.0.1 与 0.0.0.0 混为一谈**：服务监听 ${C}127.0.0.1${C} 只接受本机，监听 ${C}0.0.0.0${C} 接受所有网卡——容器/远程连不上常因绑错地址。
2. **只开防火墙没开监听**：端口不通先 ${C}ss -lntp${C} 确认程序真在听，再查防火墙。
3. **混淆公网/私网 IP**：192.168/10/172.16 是私网，外网直接连不通，要 NAT 或公网 IP。
4. **以为 TCP 是「连接」**：IP 层无连接，TCP 的连接是双方状态机的约定，不是真有根线。

## ✅ 排障清单

- [ ] 分层定位：是 DNS、端口、路由还是链路问题
- [ ] 服务监听地址（127.0.0.1 vs 0.0.0.0）确认对
- [ ] ${C}ss -lntp${C} 看是否真在监听，再看防火墙
- [ ] 分清私网/公网 IP 与 NAT
`
          },
          {
            id: "http-detail",
            title: "HTTP 协议详解",
            minutes: 15,
            updated: "2026-09-15",
            applies: "HTTP/1.1 · 2 · 3",
            tags: ["HTTP", "协议", "Web"],
            terms: ["HTTP", "状态码", "请求", "Web"],
            body: `
## 一、HTTP 是「请求-响应」无状态协议

客户端发请求（方法+URL+头+体），服务端回响应（状态码+头+体）。**无状态**指每次请求独立，服务端不记得上一次——会话状态靠 Cookie / Token 自己带。

## 二、方法语义：别把所有操作都 POST

| 方法 | 语义 | 幂等 |
|---|---|---|
| GET | 取资源，不改状态 | 是 |
| POST | 新建 | 否 |
| PUT | 整体替换 | 是 |
| DELETE | 删除 | 是 |
| PATCH | 部分更新 | 否 |

⚠ **幂等**是分布式里极重要的概念：重试 PUT/DELETE 安全，重试 POST 可能重复下单。

## 三、状态码：服务端在说什么

- **2xx** 成功（200 正常 / 201 已创建）
- **3xx** 重定向（301 永久 / 302 临时 / 304 未修改→用缓存）
- **4xx** 客户端错（400 参数错 / 401 未认证 / 403 无权限 / **404 不存在** / 429 限流）
- **5xx** 服务端错（**500 内部异常** / 502 网关错 / 503 不可用 / 504 网关超时）

## 四、Header 里那些真正影响性能与安全的

${F}http
Cache-Control: max-age=3600          # 缓存策略
Content-Type: application/json; charset=utf-8
Authorization: Bearer <token>       # 认证
Cookie: sessionid=xxx               # 会话
Connection: keep-alive              # 复用 TCP 连接
${F}

**HTTP/1.1 → 2 → 3 的关键进化**：1.1 队头阻塞（一个请求卡住整条连接）；2 多路复用（一个连接并发多请求）；3 基于 QUIC（UDP），0-RTT、连接迁移更抗弱网。

## ⚠ 踩坑与经验

1. **GET 带 Body / 缓存敏感数据**：GET 可能被缓存、记日志，敏感操作别用 GET。
2. **分不清 401 和 403**：401 是「你是谁没证明」，403 是「证明了但你没权限」。
3. **把 502/504 当业务 5xx 处理**：502 多是网关连不上后端（后端挂/超时），504 是上游太久——先查依赖。
4. **没设连接超时**：HTTP 客户端不设 connect/read timeout，下游慢时线程全阻塞。
5. **忽视 304**：做好了缓存，重复请求本可 304 省带宽，却每次都 200 全量返回。

## ✅ 排障清单

- [ ] 方法语义正确，幂等方法用于可重试场景
- [ ] 分清 4xx（客户端）与 5xx（服务端），502/504 先查依赖
- [ ] 认证用 Authorization，会话用 Cookie，敏感不进 GET
- [ ] HTTP 客户端务必设 connect/read 超时
- [ ] 善用 Cache-Control / 304 减少不必要传输
`
          },
          {
            id: "process-thread",
            title: "进程与线程",
            minutes: 13,
            updated: "2026-09-15",
            applies: "通用 OS 概念",
            tags: ["进程", "线程", "OS"],
            terms: ["进程", "线程", "并发"],
            body: `
## 一、进程和线程到底差在哪

- **进程**：资源分配的基本单位，有独立的内存空间（代码、堆、栈、文件句柄）。进程间互不干扰，但通信（IPC）成本高。
- **线程**：CPU 调度的基本单位，同一进程内的线程**共享内存**，切换便宜，但一个崩了可能拖垮整个进程。

一句话：**进程是「容器」，线程是「干活的」**。单进程多线程能轻松共享数据，代价是要注意并发安全。

## 二、上下文切换：看不见的开销

CPU 在多个线程间快速切换，制造「并行」假象（单核）或真正并行（多核）。但切换要**保存/恢复寄存器、切换页表/内存映射**，有成本。

⚠ **线程不是越多越好**：线程数超过 CPU 核数后，大量时间花在上下文切换上，吞吐量反而下降。这就是为什么有线程池（前面 Java 篇讲过）。

## 三、并行 vs 并发

- **并发（concurrent）**：多个任务交替执行，宏观同时、微观分时。
- **并行（parallel）**：多个任务真正同时跑（多核）。

${C}单核只能并发，多核才能并行${C}。写并发程序时别假设「一定会并行」。

## 四、协程/纤程：更轻的「线程」

Go 的 goroutine、Java 的虚拟线程（Project Loom）、Python 的 asyncio，都是**用户态轻量级调度**，一个线程上跑成千上万个协程，切换不进内核，成本极低。高并发网络服务由此受益巨大。

## ⚠ 踩坑与经验

1. **以为多线程必然更快**：单核 + 锁竞争激烈，多线程反而更慢（切换 + 等待锁）。
2. **共享内存不保护**：多线程改同一变量没同步，结果随机错乱（竞态）。
3. **线程数不设上限**：无脑 new，上下文切换把 CPU 吃满。
4. **混淆并行与并发**：按「并行」假设写逻辑，结果偶发错。
5. **进程间通信当共享内存用**：跨进程直接读对方内存不可能，要用管道/消息队列/共享内存段。

## ✅ 排障清单

- [ ] 进程=资源容器，线程=调度单位，共享内存要同步
- [ ] 线程数 ≈ CPU 核数（CPU 密集）/ 适当放大（IO 密集），非越多越好
- [ ] 区分并发（分时）与并行（多核）
- [ ] 高并发网络场景考虑协程/虚拟线程
- [ ] 竞态用锁/原子/无锁结构防护
`
          },
          {
            id: "memory-basics",
            title: "内存管理基础",
            minutes: 13,
            updated: "2026-09-15",
            applies: "通用 OS 概念",
            tags: ["内存", "OS", "虚拟内存"],
            terms: ["内存", "虚拟内存", "OS"],
            body: `
## 一、虚拟内存：每个进程都以为自己独占内存

现代 OS 给每个进程一个**独立的虚拟地址空间**，由 MMU 映射到物理内存。好处：① 进程互不干扰；② 程序不用管物理在哪；③ 内存不够时把不用的换到磁盘（swap）。

## 二、用户态 vs 内核态

CPU 有两种特权级：
- **用户态**：你的普通程序跑在这，不能直接碰硬件/内存管理。
- **内核态**：执行系统调用（读写文件、建网络连接）时切进来。

切换有成本。频繁系统调用（如每次循环都读写文件）会频繁陷入内核，拖慢性能。

## 三、堆与栈

| | 栈（stack） | 堆（heap） |
|---|---|---|
| 生命周期 | 随函数进出自动分配释放 | 手动/GC 管理，长期存在 |
| 速度 | 极快（指针移动） | 较慢（分配器/GC） |
| 大小 | 有限（默认几 MB） | 大（受限于内存） |

⚠ **栈溢出**：无限递归 → StackOverflowError。堆则是 OOM 的主战场（前面 JVM 篇详讲）。

## 四、缺页与 swap

访问的虚拟页不在物理内存 → **缺页中断** → 从磁盘载入。如果物理内存真不够，就得把不常用的页换到 swap 磁盘——一旦开始频繁 swap，**性能断崖下跌**（磁盘比内存慢万倍）。

## ⚠ 踩坑与经验

1. **swap 没监控**：内存满后疯狂 swap，服务从「慢」变成「假死」，si/so 指标飙升。
2. **大对象放栈**：超大局部数组撑爆栈，崩溃难查。
3. **以为虚拟内存=物理内存**：进程申请 8G 虚拟地址不代表真占 8G 物理，看 RSS/实际占用。
4. **频繁小对象分配不回收**：堆碎片 + GC 压力，前面 Java 内存篇的根因常在这。
5. **mmap 文件当内存用却不懂脏页回写**：数据可能没及时落盘，宕机丢数据。

## ✅ 排障清单

- [ ] 理解虚拟内存映射，不把虚拟地址当物理占用
- [ ] 监控 swap（si/so），避免频繁换页拖垮性能
- [ ] 区分堆（长期）与栈（自动），避免栈溢出
- [ ] 系统调用频繁 → 考虑批量，减少态切换
- [ ] 大文件用 mmap 要关注脏页回写时机
`
          }
        ]
      },
      /* ============================ 中级 ============================ */
      {
        id: "mid",
        name: "中级",
        desc: "能解释现象、定位问题：TCP 可靠与拥塞、TLS 握手、DNS/CDN、IO 多路复用与磁盘 IO。",
        chapters: [
          {
            id: "tcp-reliable",
            title: "TCP 可靠传输与拥塞控制",
            minutes: 18,
            updated: "2026-09-15",
            applies: "TCP RFC 793/5681",
            tags: ["TCP", "拥塞控制", "可靠传输"],
            terms: ["TCP", "三次握手", "拥塞", "网络"],
            body: `
## 一、TCP 凭什么「可靠」

UDP 发了就不管，TCP 保证：**不丢、不乱、不重、按序到**。靠四件套：
1. **序列号 + 确认（ACK）**：知道谁到了、谁没到
2. **超时重传（RTO）**：没收到 ACK 就重发
3. **滑动窗口**：接收方告诉发送方「我还能收多少」，流量控制
4. **拥塞控制**：网络拥堵时主动减速

## 二、三次握手与四次挥手（必考点）

**握手（建立连接）**：
${C}客户端 SYN → 服务端 SYN+ACK → 客户端 ACK${C}
为什么三次？两次不够（服务端不知道客户端收没收到自己的 ACK），四次多余。三次刚好双方都确认「我能发也能收」。

**挥手（断开连接）**：
${C}主动方 FIN → 被动方 ACK → 被动方 FIN → 主动方 ACK${C}
为什么四次？被动方收到 FIN 只能先 ACK，等自己数据发完再发 FIN——收发是两件事。

⚠ **TIME_WAIT**：主动关闭方最后会进 TIME_WAIT 持续 2MSL。作用是**等最后的 ACK 可能重传 + 让旧连接的残留报文消散**。这也是「TIME_WAIT 过多」的由来（见 OPS 故障手册）。

## 三、拥塞控制：别把网络挤爆

发送方维护一个**拥塞窗口 cwnd**，初始很小，探测网络能力：

| 阶段 | 行为 | 窗口变化 |
|---|---|---|
| 慢启动 | 每收一个 ACK，cwnd +1 | 指数增长 |
| 拥塞避免 | 每轮 cwnd +1 | 线性增长 |
| 丢包（超时） | 重置慢启动，cwnd 回到 1 | 重回指数 |
| 快速重传/恢复 | 收到 3 个重复 ACK | cwnd 折半，线性 |

新算法（**BBR**，Google）不再依赖丢包判断拥塞，直接建模带宽和延迟，高丢包弱网下吞吐远好于传统。

## ⚠ 踩坑与经验

1. **把 TCP 当「消息」协议用**：TCP 是字节流，没有消息边界，应用层要自己分包（长度前缀/分隔符）。
2. **忽略了 Nagle / 延迟确认导致的请求-应答卡顿**：小包交互场景（如游戏）要 ${C}TCP_NODELAY${C} 关掉 Nagle。
3. **TIME_WAIT 耗尽端口**：高并发短连接客户端常见，前面故障手册给了解法（连接池/复用）。
4. **混淆流量控制与拥塞控制**：前者防「压垮接收方」，后者防「压垮网络」。
5. **以为丢包=网络差**：可能是对端处理不过来（接收窗口满），也可能是真丢包。

## ✅ 排障清单

- [ ] 理解三次握手/四次挥手的必要性
- [ ] TIME_WAIT 是正常设计，端口耗尽才处理（连接池优先）
- [ ] 小包交互开 TCP_NODELAY
- [ ] 高丢包弱网考虑 BBR 拥塞算法
- [ ] 应用层自己处理 TCP 字节流的分包
`
          },
          {
            id: "https-tls",
            title: "HTTPS / TLS 握手",
            minutes: 16,
            updated: "2026-09-15",
            applies: "TLS 1.2 / 1.3",
            tags: ["HTTPS", "TLS", "安全"],
            terms: ["HTTPS", "TLS", "证书", "加密"],
            body: `
## 一、HTTPS = HTTP + TLS 加密层

HTTP 明文传输，中间人能窃听篡改。TLS 在 TCP 之上加一层加密，提供**机密性（加密）、完整性（防篡改）、身份认证（证书）**。

## 二、非对称 + 对称混合：性能与安全的平衡

- **非对称加密（RSA/ECC）**：公钥加密、私钥解密，慢，用于「协商」
- **对称加密（AES）**：双方同一把密钥，快，用于「传输正文」

TLS 的聪明处：**握手阶段用非对称交换出一把「会话密钥」，之后全程用对称加密传数据**。既安全又高效。

## 三、TLS 1.2 握手要点（简化）

${C}1. ClientHello：支持的加密套件、随机数\n2. ServerHello：选定套件 + 服务端随机数 + 证书(含公钥)\n3. 客户端验证证书 → 用公钥加密「预备主密钥」发给服务端\n4. 双方各自算出会话密钥\n5. 之后对称加密通信${C}

**证书验证**是关键：客户端用内置 CA 公钥验证证书签名链，确认「这真是 example.com，不是冒充的」。证书过期/域名不匹配/不可信 CA → 浏览器红警告。

## 四、TLS 1.3 更快更安全

- 握手 **1-RTT**（1.2 是 2-RTT），支持 **0-RTT** 恢复
- 砍掉不安全的加密算法（RC4、RSA 密钥交换等）
- 前向安全成为默认

## ⚠ 踩坑与经验

1. **证书过期全站 HTTPS 失败**：前面故障手册讲了——必须做有效期监控 + 自动续期（cert-manager / acme.sh）。
2. **自签证书没装信任链**：客户端不信任，报错。内网要先把 CA 加进信任库。
3. **只在入口加密，内网明文**：通过网关解密后内网裸奔，内网被渗透照样泄露，按合规要求决定是否全程加密。
4. **混合内容（Mixed Content）**：HTTPS 页面里嵌 HTTP 资源，被浏览器拦截。
5. **没开 HSTS**：用户第一次访问仍可能被劫持到 HTTP，配 HSTS 强制 HTTPS。

## ✅ 排障清单

- [ ] 证书有效期监控 + 自动续期，域名/SAN 匹配
- [ ] 证书链完整（含中间 CA），否则客户端不信任
- [ ] TLS 1.3 优先，禁用老旧不安全算法
- [ ] 配 HSTS，避免 Mixed Content
- [ ] 内网是否需加密按合规定，别只防外网
`
          },
          {
            id: "dns-cdn",
            title: "DNS 与 CDN",
            minutes: 15,
            updated: "2026-09-15",
            applies: "DNS / Anycast CDN",
            tags: ["DNS", "CDN", "网络"],
            terms: ["DNS", "CDN", "域名解析"],
            body: `
## 一、DNS：把域名翻译成 IP

你访问 example.com，浏览器先问 DNS「这名字对应哪个 IP」。递归解析过程：

${C}浏览器缓存 → 本地 hosts → 本地 DNS  resolver → 根 → 顶级域(.com) → 权威 DNS → 拿到 IP${C}

## 二、记录类型（常考）

| 类型 | 作用 |
|---|---|
| A | 域名 → IPv4 |
| AAAA | 域名 → IPv6 |
| CNAME | 域名别名（指向另一个域名） |
| MX | 邮件服务器 |
| TXT | 文本，常放 SPF / 验证信息 |
| NS | 该域的权威 DNS 服务器 |

⚠ **CNAME 不能和同名的其他记录共存**，且根域名通常不能用 CNAME（用 ALIAS/NS 替代），这是配置翻车的重灾区。

## 三、TTL：缓存的双刃剑

每条解析记录有 TTL（生存时间），期内大家都缓存，不再问。改 DNS 后**最长要等 TTL 过期才全网生效**。紧急切换时把 TTL 提前调小（如 60s），改完再调大。

## 四、CDN：让用户就近取内容

CDN 把静态资源（图片/JS/CSS/视频）缓存到**离用户近的边缘节点**。用户请求被调度到最近节点，源站压力骤降、访问变快。

**调度靠 DNS**：CDN 的智能 DNS 根据你的来源 IP 返回最近的边缘节点 IP。还有 Anycast（同一 IP 多地广播，路由自动选近的）。

## ⚠ 踩坑与经验

1. **改了解析半天不生效**：TTL 没到，全世界还在用旧缓存。紧急切换前先降 TTL。
2. **resolv.conf 配了不可达 DNS**：每次解析都等超时，接口整体变慢（前面故障手册讲过）。
3. **CNAME 配置冲突**：根域名或与其他记录冲突导致解析失败。
4. **CDN 回源被压垮**：CDN 大面积失效回源，源站直接被打挂。源站也要限流+缓存。
5. **缓存没区分版本**：静态资源改了但 CDN 还返旧版。用「文件名带 hash / 加版本 query」破缓存。

## ✅ 排障清单

- [ ] 紧急变更前把 TTL 调小，变更后恢复
- [ ] resolv.conf 的 DNS 必须可达
- [ ] 避免 CNAME 冲突（根域不用 CNAME）
- [ ] CDN 回源要有源站保护与缓存策略
- [ ] 静态资源版本化（hash/query）破缓存
`
          },
          {
            id: "io-multiplexing",
            title: "IO 多路复用",
            minutes: 17,
            updated: "2026-09-15",
            applies: "Linux epoll / select / poll",
            tags: ["IO", "epoll", "多路复用"],
            terms: ["IO", "epoll", "多路复用", "网络"],
            body: `
## 一、问题的起点：一个线程怎么管上万连接

早期模型：一个连接一个线程（BIO）。1 万连接 = 1 万线程，上下文切换直接把机器拖死。

**IO 多路复用**：**一个线程同时盯着一堆 fd，谁就绪就处理谁**，不用为每个连接配一个线程。

## 二、select / poll / epoll 演进

| 机制 | 原理 | 缺陷 |
|---|---|---|
| select | 把 fd 集合传给内核，遍历 | fd 数量有限(1024)、每次全量拷贝+遍历 |
| poll | 类似 select，用链表去掉 1024 限制 | 仍需全量遍历，大连接下慢 |
| **epoll** | 内核维护就绪链表，只返回就绪的 | 海量连接下依然高效（事件驱动） |

**epoll 的三大优势**：① 内核红黑树管理 fd，增删查 ${C}O(1)${C}；② 只返回就绪 fd，不遍历全部；③ 支持边缘触发（ET）和水平触发（LT）。

## 三、两种触发模式

- **LT（水平触发）**：只要 fd 没处理完就一直通知，稳妥但可能重复通知
- **ET（边缘触发）**：状态变化才通知一次，效率高但必须**一次读/写到 EAGAIN**，否则会漏事件。用 ET 的坑最多。

## 四、这跟上层有什么关系

Nginx、Redis、Node.js、Netty 的底层都是 epoll（Linux）。理解它，你就懂了**为什么 Redis 单线程能扛高并发**（纯内存 + 非阻塞 IO + epoll，没有锁竞争），以及 **C10K/C10M 问题**怎么被解决。

## ⚠ 踩坑与经验

1. **ET 模式没读干净**：只读了部分数据，后续事件不再触发，连接「假死」。ET 必须循环读到 EAGAIN。
2. **把阻塞 IO 配进事件循环**：事件循环里任何一个同步阻塞调用都会卡住整个循环，所有连接都跟着等。
3. **select 硬撑大并发**：连接数上千就换 epoll（或用封装好的库）。
4. **忘记非阻塞**：多路复用配合的 fd 必须设非阻塞，否则读不到数据就卡住线程。
5. **以为多路复用 = 多线程**：它解决「一个线程管多连接」，compute 密集仍要配合线程池。

## ✅ 排障清单

- [ ] 海量连接用 epoll（或 NIO 框架），不用 select
- [ ] ET 模式循环读到 EAGAIN，LT 更稳妥
- [ ] 事件循环里禁阻塞调用，fd 设非阻塞
- [ ] 理解 Redis/Node 高性能的根因是多路复用
- [ ] compute 密集配合线程池，别只靠单线程
`
          },
          {
            id: "fs-disk-io",
            title: "文件系统与磁盘 IO",
            minutes: 15,
            updated: "2026-09-15",
            applies: "Linux 文件系统",
            tags: ["文件系统", "磁盘", "IO"],
            terms: ["磁盘", "IO", "文件系统", "inode"],
            body: `
## 一、VFS：屏蔽底层差异的统一抽象

Linux 用**虚拟文件系统（VFS）**把 ext4/xfs/overlay 等不同文件系统统一成一套接口（open/read/write/close）。应用不关心底层是啥，这也是「一切皆文件」的基础（前面 OPS 篇的 ${C}/proc${C}、${C}/dev${C} 都挂在 VFS 下）。

## 二、inode：文件的真身

磁盘上每个文件对应一个 **inode**，存元信息（权限、大小、时间戳、数据块指针），**不含文件名**。文件名只是目录项（dentry）到 inode 的映射。

⚠ **这就是为什么「删了文件但空间不释放」**：进程还持着文件句柄（引用计数>0），inode 没真正回收，空间不释放。前面 OPS 故障手册的 ${C}lsof | grep deleted${C} 就是查这个。

## 三、页缓存（Page Cache）：读写都很快的真相

读文件时，内核把磁盘内容缓存在内存页缓存里，下次读直接命中内存。写默认也先写页缓存（write-back），异步刷盘——所以 ${C}write()${C} 返回快不等于数据落盘了。

${C}sync${C} 强制刷盘；数据库/重要数据用 ${C}O_DIRECT${C} 或 ${C}fsync${C} 保证持久化（代价是慢）。

## 四、顺序 IO vs 随机 IO

机械盘顺序读写远高于随机（磁头寻道成本高）。SSD 没这个问题但仍受益。所以：
- 日志/数据文件**顺序追加**远快于随机更新
- 数据库用 WAL（预写日志）把随机写转成顺序写

## ⚠ 踩坑与经验

1. **以为 write 成功就持久**：没 fsync，宕机丢数据。重要写入必须 fsync/开启刷盘策略。
2. **inode 耗尽**：海量小文件（如会话、碎片）把 inode 用光，df 显示有空间却写不进（OPS 篇 df -i）。
3. **删大文件用 rm 不释放空间**：进程还持有，用 ${C}> file${C} 截断或重启进程。
4. **在容器 overlay 层猛写**：overlayfs 写放大，且占满影响同节点其他容器，日志/临时文件要挂 volume。
5. **同步写到处用**：每条日志都 fsync，性能崩。批量 + 定时刷盘更合理。

## ✅ 排障清单

- [ ] 重要写入 fsync 保证持久，别信 write 返回
- [ ] 空间写不进先 df -i 查 inode
- [ ] 删文件不释放 → lsof | grep deleted，必要时截断
- [ ] 大文件/日志用顺序追加；随机写转 WAL
- [ ] 容器写操作挂 volume，避免 overlay 写放大
`
          }
        ]
      },
      /* ============================ 高级 ============================ */
      {
        id: "adv",
        name: "高级",
        desc: "能解决复杂底层问题：内核网络栈调优、高并发网络模型、容器网络原理与系统调用剖析。",
        chapters: [
          {
            id: "kernel-net-tune",
            title: "内核网络栈与调优",
            minutes: 18,
            updated: "2026-09-15",
            applies: "Linux 内核网络",
            tags: ["内核", "网络调优", "参数"],
            terms: ["网络", "内核", "TCP", "调优"],
            body: `
## 一、数据包在内核里走了多远

收到一个包：网卡 DMA → 硬中断 → 软中断（NAPI 收包）→ IP 层 → TCP 层重组 → 拷贝到用户态 socket 缓冲区 → 应用 read。

**每一步都可能成为瓶颈**：中断太多（网卡小包风暴）、软中断集中某核、上下文切换多。

## 二、最常被调的几个参数

${F}bash
# 端口范围（短连接客户端 TIME_WAIT 多时不够用）
sysctl -w net.ipv4.ip_local_port_range="1024 65535"

# TIME_WAIT 复用（仅客户端有效），NAT 环境别开 tw_recycle
sysctl -w net.ipv4.tcp_tw_reuse=1

# 全连接队列上限（accept 队列），高并发要调大
sysctl -w net.core.somaxconn=65535

# backlog / 半连接队列
sysctl -w net.ipv4.tcp_max_syn_backlog=65535

# 内存缓冲区（TCP 收发），影响长肥管道吞吐
sysctl -w net.ipv4.tcp_rmem="4096 87380 67108864"
sysctl -w net.ipv4.tcp_wmem="4096 65536 67108864"
${F}

⚠ **队列溢出是「连接被拒/超时」的隐蔽元凶**：全连接队列（somaxconn）太小，瞬时高并发时 accept 不过来，新连接直接被丢弃或重传。

## 三、多队列与中断亲和

现代网卡多队列 + RSS（接收端缩放），把不同流哈希到不同队列、绑到不同 CPU 核，**避免单核软中断打满**。用 ${C}irqbalance${C} 或手动绑核。

## 四、调优的正确姿势

**先测后调，一次一个**：用 ${C}ss${C}、/${C}proc/net${C}、火焰图、压测确认瓶颈在哪个环节，再针对性改，改完复测。别抄一份「万能内核参数」无脑套——别人的瓶颈不是你的瓶颈。

## ⚠ 踩坑与经验

1. **盲开 tcp_tw_recycle**：NAT 环境下导致大量连接被随机丢弃（内核 4.12+ 已移除），用 tw_reuse 足够。
2. **somaxconn 没调，并发一高就丢连接**：网关/Proxy 类服务尤甚。
3. **调了参数没持久化**：sysctl -w 重启失效，要写进 ${C}/etc/sysctl.conf${C} 或 systemd-sysctl。
4. **单核软中断打满**：没开多队列/RSS，千兆网卡小包就能把一核吃满。
5. **buffer 调太大反而引入延迟**：rmem/wmem 过大增加延迟，按业务权衡。

## ✅ 排障清单

- [ ] 队列（somaxconn / syn_backlog）按并发量调大
- [ ] 多队列 + RSS + 中断亲和，避免单核瓶颈
- [ ] 只在客户端用 tcp_tw_reuse，不开 tw_recycle
- [ ] 参数改完写 sysctl.conf 持久化
- [ ] 调优前先量化瓶颈，一次改一处并复测
`
          },
          {
            id: "high-conn-model",
            title: "高并发网络模型",
            minutes: 17,
            updated: "2026-09-15",
            applies: "C10K / C10M 模型",
            tags: ["高并发", "网络模型", "架构"],
            terms: ["高并发", "epoll", "网络模型", "IO"],
            body: `
## 一、从 C10K 到 C10M

- **C10K**（1 万并发连接）：传统「一连接一线程」撑不住，催生 epoll/事件驱动（Nginx、Node）。
- **C10M**（1 千万）：光靠内核协议栈也不够了，要在**内核旁路（DPDK / XDP / eBPF）**和用户态协议栈上做文章。

## 二、主流并发模型对比

| 模型 | 代表 | 思路 | 适用 |
|---|---|---|---|
| 多进程 | 早期 Apache prefork | 一连接一进程 | 稳定但重 |
| 多线程阻塞 | 传统 BIO 服务器 | 一连接一线程 | 连接少 |
| 事件驱动（Reactor） | Nginx / Node / Redis | 单/少线程 + epoll | 高并发 IO |
| 协程（用户态） | Go / Java 虚拟线程 | 海量轻量协程 | 高并发 + 易写 |
| 内核旁路 | DPDK 应用 | 绕开内核协议栈 | 极致吞吐（NFV/网关） |

## 三、Reactor 模式：事件驱动的骨架

${C}多路复用器(epoll) 监听事件 → 事件分发器 → 具体的 Handler 处理（读/算/写)${C}

单 Reactor 单线程（Redis 主线程）、单 Reactor 多线程（把 compute 丢线程池）、多 Reactor（主 Reactor 管 accept，子 Reactor 管 IO，Netty 就是这模式）。

## 四、为什么 Go 写高并发这么爽

Go 的 goroutine 是**用户态调度**，每个只有几 KB 栈，一个进程跑几十万协程，写起来还是「同步顺序」的代码风格，调度器自动映射到 OS 线程（GMP 模型）。你不用自己管 epoll 和线程池，心智负担大幅下降。

## ⚠ 踩坑与经验

1. **在高并发服务里用阻塞 IO**：一个同步读卡住整个事件循环，所有连接跟着等。
2. **线程/协程数无脑堆**：上下文切换成本回来找你，吞吐量不升反降。
3. **忘了对接背压（backpressure）**：上游猛灌、下游处理慢，内存被冲爆。要限流/队列/丢弃。
4. **把单线程 Reactor 当万能**：单线程算密集任务会阻塞所有 IO，compute 要丢线程池。
5. **盲目追求 C10M 内核旁路**：大部分业务到不了那量级，普通 epoll + 合理架构足够，先别过度设计。

## ✅ 排障清单

- [ ] 按场景选模型：IO 密集用事件驱动/协程，compute 密集配线程池
- [ ] 事件循环禁阻塞调用，重活丢线程池/协程池
- [ ] 对接背压/限流，防内存被冲爆
- [ ] 高并发用 epoll/NIO，别用一连接一线程
- [ ] 没到极致吞吐别上内核旁路，避免过度设计
`
          },
          {
            id: "container-net",
            title: "容器网络原理",
            minutes: 17,
            updated: "2026-09-15",
            applies: "Docker / Kubernetes 网络",
            tags: ["容器", "网络", "CNI"],
            terms: ["容器", "网络", "Docker", "CNI"],
            body: `
## 一、容器网络的本质：独立的网络空间

容器用 **Linux 命名空间（namespace）** 隔离，包括网络命名空间——每个容器有自己的网卡、IP、路由表、端口。问题和宿主机「隔离」了，所以容器间、容器与外面通信需要额外桥接。

## 二、Docker 的几种网络模式

| 模式 | 说明 | 典型用途 |
|---|---|---|
| bridge（默认） | 容器连到 docker0 网桥，通过 NAT 出网 | 常规应用 |
| host | 共享宿主机网络命名空间 | 性能敏感、要裸端口 |
| none | 无网络 | 离线计算 |
| container | 共享另一个容器的网络 | 边车/调试 |

## 三、K8s 的网络三约法

K8s 要求：**Pod 间直接互通（扁平网络）、Pod 干掉 IP 不变、Service 有稳定虚拟 IP**。实现靠 **CNI 插件**：

- **Flannel**：简单 overlay，性能一般，适合小集群
- **Calico**：BGP 路由 / 支持 NetworkPolicy，性能好
- **Cilium**：基于 eBPF，可观测性与安全强，新项目首选

⚠ **Service 通过 iptables / IPVS 做负载均衡**：集群规模大时 iptables 规则膨胀，规则匹配变慢，IPVS 模式更稳（前面 OPS K8s 篇提过）。

## 四、跨主机流量怎么走

Pod A → 通过 veth pair 进宿主机网桥 → 经过 CNI 的 overlay/路由 → 到达节点 B → 进 Pod B。中间每一跳都可能丢包（MTU 不匹配、路由错、网络策略拦了）。

## ⚠ 踩坑与经验

1. **两个容器端口冲突**：同一 host 网络模式下的两个容器监听同端口会冲突，用 bridge 模式各占命名空间。
2. **MTU 不匹配导致偶发丢包/慢**：overlay 封装后 MTU 变小，没相应调整会分片或丢大包，疑难网络问题先查 MTU。
3. **NetworkPolicy 没配默认全通**：安全风险 + 故障易扩散，生产应显式白名单。
4. **Service 没就绪就调**：Endpoint 还没挂上，访问 Service IP 连不上，先看 endpoints 是否就绪。
5. **Pod 用 IP 硬编码**：Pod IP 会变，必须靠 Service/DNS 名字访问。

## ✅ 排障清单

- [ ] 容器网络模式按需选（bridge/host/none）
- [ ] K8s 用 CNI 实现扁平网络，大规模用 IPVS
- [ ] 跨节点先查 MTU / 路由 / NetworkPolicy
- [ ] 访问走 Service/DNS 名字，不硬编码 Pod IP
- [ ] 生产显式 NetworkPolicy 白名单
`
          },
          {
            id: "syscall-profiling",
            title: "系统调用与性能剖析",
            minutes: 16,
            updated: "2026-09-15",
            applies: "Linux perf / eBPF / strace",
            tags: ["性能", "剖析", "syscall"],
            terms: ["性能", "syscall", "strace", "perf"],
            body: `
## 一、系统调用：用户程序与内核的边界

你写的 ${C}read/write/open/connect${C} 不是直接操作硬件，而是**陷入内核（syscall）**由内核代为完成。每次 syscall 都有上下文切换成本；频繁 syscall 是性能隐形杀手。

## 二、strace：看程序到底在调什么

${F}bash
strace -p <pid>                      # 实时看某进程的系统调用
strace -c -p <pid>                  # 统计各类 syscall 的次数/耗时
strace -f -e trace=network ./app    # 只跟踪网络相关调用
${F}

经典发现：一个程序 CPU 不高但慢，strace 一看——每秒几万次 ${C}stat()${C} / ${C}open()${C}，全是无效的文件存在性检查。

## 三、perf：CPU 热点与火焰图

${F}bash
perf top -p <pid>                   # 实时看 CPU 花在哪
perf record -F 99 -p <pid> -g -- sleep 30   # 采样 30s
perf script | flamegraph > flame.svg        # 生成火焰图
${F}

**火焰图**横轴采样、纵轴调用栈，最宽的「平顶」就是最耗时的函数。比猜强一万倍。

## 四、eBPF：不改代码看清内核态

eBPF 能在内核里安全地挂探针，观测 syscall、调度、IO 延迟，**几乎零开销**。工具如 **bcc / bpftrace / pixie**。比如一行脚本看某进程的 TCP 重传、某文件的 IO 延迟。

## ⚠ 踩坑与经验

1. **strace 上生产忘了关**：strace 会显著拖慢被跟踪进程（尤其 -f 跟踪多线程），用完立刻停。
2. **只盯用户态 CPU**：慢其实在内核态（syscall 多 / 锁 / IO 等待），要 ${C}perf${C} + ${C}syscall${C} 维度一起看。
3. **频繁无谓 syscall**：循环里查文件、重复 gettimeofday，攒起来很可观，批量/缓存掉。
4. **火焰图看不懂方向**：纵轴是「调谁」，横轴是「占比」，找最宽平顶，别被最顶上的小尖迷惑。
5. **perf 没符号**：很多程序 strip 了符号，火焰图全是问号，编译时保留 debug 符号（或单独存）。

## ✅ 排障清单

- [ ] 先用 strace -c 看 syscall 次数/耗时分布
- [ ] perf + 火焰图定位 CPU 热点（含内核态）
- [ ] 频繁无谓 syscall 批量/缓存化，减少态切换
- [ ] 生产 strace 用完即停，避免拖慢
- [ ] eBPF 做低开销内核态观测，编译保留符号
`
          }
        ]
      }
    ]
  };

  window.NETWORK = NETWORK;
})();
